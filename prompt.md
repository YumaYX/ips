Add Rustdoc comments suitable for `cargo doc`.

Requirements:

* Document all public items (`pub` structs, enums, traits, functions, constants, type aliases).
* Use `///` for item documentation and `//!` for module documentation when appropriate.
* Focus on purpose, usage, parameters, return values, errors, panics, and safety requirements.
* Add concise examples where helpful.
* Preserve existing comments and improve them if necessary.
* Do not change behavior or logic.
* Return the complete updated Rust code only.
